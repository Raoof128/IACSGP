# Example Terraform Backend Configuration
# Uncomment and configure for remote state management

# terraform {
#   backend "s3" {
#     bucket         = "my-terraform-state-bucket"
#     key            = "iac-security/terraform.tfstate"
#     region         = "us-west-2"
#     encrypt        = true
#     dynamodb_table = "terraform-state-lock"
#   }
# }

# For local development, comment out the backend block
# Terraform will use local state file
